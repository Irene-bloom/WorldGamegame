# 开发日志 DEVLOG · 《小径分岔的花园》

> 逐步记录每一次改动、原因和结论，方便后续复盘。**最新记录在最上面。**

---

## 2026-06-06 · 步骤 7：收尾 + 项目复盘

- **做了什么**：
  - 完善 `README.md`：玩法说明、核心机制、技术栈、运行/验证方式、代码结构、如何扩展（加关卡 + 接 AI）。
  - 写本节最终复盘。
- **项目整体复盘**：
  - **目标达成**：从「想做个有世界观+地图探索的游戏」的模糊需求出发，最终落地为一个**可通关、机制成立、有博尔赫斯气质**的解谜原型。MVP 的全部范围（2 条时间线、10+ 节点、2 个跨线推理谜题、可视化分岔网、可通关、可存档）都完成了。
  - **关键决策回顾**：
    1. 拒绝「魔法学院」套路，转向博尔赫斯式「有严密逻辑的奇幻」——是这个项目的灵魂。设定本身即谜题。
    2. 选《小径分岔的花园》+「跨线观察推理」机制——独特且没人做过，且纯文字/静态就能实现，适合练手。
    3. 先纯静态、数据驱动——保证谜题可解、可回归测试，把「加内容」和「改逻辑」解耦。
    4. 复用 weeklygame 的 Next.js 栈——省去脚手架踩坑。
  - **做对的地方**：
    - 数据驱动 + 纯函数引擎，让端到端验证（`npm run verify`，16 断言）变得可能，可通关性有了硬保证而非「我觉得能过」。
    - `lib/narrator.ts` 接口预留，未来接 AI 不动 UI。
    - 每个里程碑一次 commit + DEVLOG 记录，历史清晰可复盘。
  - **踩过的坑**：
    - `npm run dev` 因 PATH 找不到 `next` 而静默失败，curl 却拿到 200（其实是 weeklygame 占着 3000）。教训：验证端口归属，用项目本地二进制 `node ./node_modules/next/.../next`。
  - **下一步可做的方向**（超出本次 MVP）：
    - 接 AI 润色叙事（接口已留）。
    - 加更多时间线/关卡，引入「多线同步状态」类更难的谜题。
    - 音效与美术氛围（参考《空洞骑士》的环境叙事）。
- **结论**：第一关《两扇门与一口井》可玩、可通关、机制经验证成立。原型 v0.1 完成。

---

## 2026-06-06 · 步骤 6：端到端通关验证（可通关 ✓）

- **做了什么**：
  - 写了 `scripts/verify.ts` —— 直接在纯函数引擎上模拟玩家操作的端到端测试，并加了 `npm run verify` 脚本（编译到 `.verify-out` 后用 node 跑，已 gitignore）。
  - **16 项断言全部通过**，覆盖：
    - [A] 正确通关路径：start → 观察左门死讯 → 右门 → 观察暗格真相 → 走暗格 → 抵达中心，`reached=true`。
    - [B] 死路1（左门）被标记 + 能回溯到 corridor。
    - [C] 死路2（盲目下井）被标记 + 能回溯到 well_room。
    - [D] **机制硬证明**：未观察邻近线时，井厅唯一可选项就是「下井」（死路）——即「盲猜必死，必须观察+推理才能通关」。
  - 进阶谜题的门控也被证明：`requiresObserved` 让「走暗格」在未观察时锁住、观察后解锁。
- **为什么这么做**：
  - 游戏逻辑全在纯函数里，用脚本模拟通关比手点浏览器更可靠、可重复、可回归。改故事数据后跑一次 `npm run verify` 就能确认没把关卡改成死局。
- **结论 / 下一步**：
  - 第一关确认**可通关**，且「跨线观察推理」机制成立（不观察就走不到正解）。核心 MVP 完成。下一步（步骤 7）收尾：完善 README、写最终 DEVLOG 复盘。

---

## 2026-06-06 · 步骤 5：时间分岔网可视化（ForkGraph）

- **做了什么**：
  - `lib/story.ts`：新增 `Edge` 类型和 `getEdges(level)`，从节点的 `choices`/`observe` 推导出所有边（choice 边 / observe 边）。
  - `components/ForkGraph.tsx`：用 **SVG**（viewBox 0–100）画出时间分岔网。节点按 `pos` 百分比布局；choice 边=绿色实线贝塞尔曲线，observe 边=琥珀虚线（连向邻近线）。节点状态分色：当前（脉冲光环）、已访问（实心亮）、眼前可选目标（空心）、死路（灰+✕）、中心（紫+星芒）。
  - **迷雾机制**：只显示玩家「已知」的节点（走过的 + 已观察的邻近线 + 当前可选目标），未知部分不画，保留探索感。
  - `app/page.tsx`：把分岔网图放到左列顶部 + 图例（当前线/邻近线/中心/死路）。新增**「跳读查看」**——点击图上任意已知节点，临时查看其叙事（带「↩ 回到我所在的当下」），不改变进度。右列观察面板始终对应真实当前节点。
  - `components/NodePanel.tsx`：加 `readOnly` 模式，跳读时只展示叙事、不显示可操作按钮。
- **验证**：3100 端口 `Compiled / in 5.2s (591 modules)`，HTTP 200，无编译错误；ForkGraph 代码（viewBox/onPeekNode/时间分岔网）确认进 `app/page.js`。`tsc --noEmit` 通过。
- **为什么这么设计**：
  - SVG 纯手写、不引第三方图库 —— 轻量可控，符合「能玩的原型」定位。
  - 「迷雾 + 跳读」让分岔网既是导航也是叙事载体：玩家能直观看到自己在时间之网中的位置、哪些路通向死亡、中心在何方，呼应博尔赫斯「时间是一张分岔的网」的母题。
- **结论 / 下一步**：
  - 可视化完成，游戏「立体」起来了。下一步（步骤 6）端到端打磨：完整走一遍通关流程，检查 2 个谜题的可解性与回溯体验，微调文案与边界情况，确保「可通关」。

---

## 2026-06-06 · 步骤 3+4：叙事交互 + 跨线观察（游戏可玩了）

- **做了什么**：
  - `components/NodePanel.tsx`：当前节点的叙事正文（段落化 + `**强调**` 渲染）+ 选择按钮 + 死路「退回分岔点」按钮 + 中心「重走」按钮。被 `requiresObserved` 锁住的进阶选项显示为「🔒 ？？？（缺少线索）」。
  - `components/ObservePanel.tsx`：**核心机制 UI**。在观察点可「◌ 侧耳倾听这条分岔」，点击后窥见邻近线的命运（线索浮现动画），已观察的线索留存为琥珀色卡片。非观察点显示「这里听不到别的时间线」。
  - `app/page.tsx`：把数据层和两个面板串成可玩游戏。双栏布局（左叙事+选择 / 右跨线观察），顶栏显示进度与「重走」，localStorage 自动存档，通关横幅。处理了 SSR/CSR 水合（首屏占位「花园正在展开」）。
- **验证**（实际跑起来确认）：
  - 用项目本地 `next` 在 3100 端口启动（3000 被 weeklygame 占着）。`Ready in 5.6s`，HTTP 200。
  - SSR 首屏正确显示占位；故事数据（「沿石廊向前走」）确认打包进客户端 `app/page.js`，水合后正常渲染。
  - `tsc --noEmit` 通过，0 错误。
- **踩坑记录**：一开始 `npm run dev` 因 PATH 找不到 `next` 而失败，但 curl 却拿到 200——排查发现 3000 端口是之前 weeklygame 的进程。改用 `node ./node_modules/next/dist/bin/next dev -p 3100` 直接调本地 next，避开 PATH 问题和端口冲突。
- **为什么这么设计**：
  - 左右双栏把「当前线处境」和「邻近线线索」并置，让「跨线观察推理」的玩法一目了然。
  - 叙事文本走 `lib/narrator.ts` 接口取，以后接 AI 润色不用动 UI。
- **结论 / 下一步**：
  - 核心玩法跑通、可通关。下一步（步骤 5）做 `ForkGraph` —— 用 SVG 把时间分岔网画出来：节点按坐标布局、两条时间线分色、走过的路径点亮、死路标灰、中心高亮，让游戏「立体」起来。

---

## 2026-06-06 · 步骤 2：数据层（时间分岔图 + 游戏状态 + 第一关）

- **做了什么**：
  - `lib/story.ts`：定义了整套时间分岔图的数据模型（`StoryNode` / `Choice` / `ObservePeek` / `Level`），并写好**第一关《两扇门与一口井》**的完整静态数据。
  - `lib/game-state.ts`：游戏状态 `GameState`（当前节点 / 已访问 / 已观察 / 死路 / 是否通关）+ localStorage 存取（`loadState`/`saveState`/`clearState`）+ 核心引擎纯函数（`applyChoice` 选择、`applyObserve` 观察、`backtrack` 回溯、`isChoiceAvailable` 进阶选项门控）。
  - `lib/narrator.ts`：叙事润色接口（预留）。第一版直接返回静态文本，未来接 AI 只改这里，不动 UI/引擎。
  - 类型检查 `tsc --noEmit` 通过，0 错误。
- **第一关的谜题设计（核心）**：
  - **谜题 1（入门）**：在「长廊分岔」观察点，能窥见邻近线里「另一个你推左门被落石压死」→ 推理出本线该选**右门**。不看线索就是盲猜。
  - **谜题 2（进阶 / 世界规则 R2 对称）**：在「井厅」观察点，能窥见邻近线里「井是假的诱饵，合上井盖会露出暗格」→ 正确解是**合井盖走暗格**而非下井。关键：「走暗格」选项用 `requiresObserved` 锁住，**必须先窥见邻近线线索才会出现**，逼玩家真去观察、推理。
- **为什么这么设计**：
  - 数据驱动：游戏逻辑只读这份数据，「加内容 = 加数据」，不改引擎。
  - 谜题严密可解：不看邻近线=盲猜，看了=能唯一推出答案——这正是「跨线观察推理」机制成立的证明。
  - 回溯而非 Game Over：走死路退回上一个分岔点，契合「所有时间线并存」的世界观。
- **结论 / 下一步**：
  - 数据层和引擎就绪、类型干净。下一步（步骤 3）做叙事交互：`NodePanel` + `page.tsx`，先让游戏能读节点、点选择、在节点间跳转跑起来（暂不画分岔网图）。

---

## 2026-06-06 · 步骤 1：项目脚手架 + Git 对接

- **做了什么**：
  - 在 `worldgame/` 下搭起 Next.js 15 + React 19 + TypeScript + Tailwind 的脚手架（复用 weeklygame 的成熟配置）。
  - 配置文件：`package.json`、`next.config.js`、`postcss.config.js`、`tailwind.config.ts`、`tsconfig.json`、`.gitignore`、`.env.example`。
  - 应用骨架：`app/layout.tsx`、`app/globals.css`、`app/page.tsx`（临时占位首页）。
  - 自定义了一套「夜色花园」主题配色：深绿黑底色 + 两条时间线的颜色（青 `alpha` / 琥珀 `beta`）+ 中心微光紫 `center`。
  - 初始化 git，对接远程空仓库 `https://github.com/Irene-bloom/WorldGamegame`。
  - 建立本开发日志 `DEVLOG.md`。
- **为什么**：
  - 复用已验证的技术栈，避免从零踩坑，让用户也熟悉。
  - 主题配色换成花园/时间迷宫风格，和《小径分岔的花园》的世界观契合，而不是照搬周目的紫色。
  - 先跑通一个能 `npm run dev` 的空项目，确认地基没问题，再往上堆功能。
- **结论 / 下一步**：
  - 脚手架就绪。下一步（步骤 2）写数据层：`lib/story.ts`（时间分岔图的类型定义 + 第一关静态数据）和 `lib/game-state.ts`（游戏状态 + localStorage 存取）。
